<%= form_for(@course) do |f|%>

    <%= render 'shared/error_messages', object: f.object %>

    <%=f.label :name%>
    <%=f.text_field :name%>

    <%= f.submit @course.id.blank? ? "Создать новый курс" : "Редактировать курс", class: "btn btn-large btn-primary" %>

<% end %>